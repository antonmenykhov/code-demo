import type { Column } from 'tnnc-ui-kit'

const formatGeneralContract: Column[] = [
  {
    name: 'name',
    columnType: 'string',
    caption: 'Наименование',
    editable: true
  }
]
export { formatGeneralContract }
