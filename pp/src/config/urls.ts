const base_url = 'http://localhost'
const supp_url = `${base_url}:8081`
const contracts_url = `${base_url}:8085/contracts`
const helpservice_url = `${base_url}:8085/helpService`
const pubsub_url = `${base_url}:8084`
const emp_moderator_url = `${base_url}:8085/emp-moderator`
export { supp_url, contracts_url, helpservice_url, pubsub_url, base_url, emp_moderator_url }
